<h1 align="center">Players</h1>
<div align="center">
  <%= will_paginate %>
    <% @users.each do |user| %>
      <ul class="listing">
        <div class="row">
          <% if user != current_user %>
            <div class="well col-md-4 col-md-offset-4">
              <li><%=  link_to image_tag(user.avatar.url(:thumb)), user_path(user) %></li>
              <li class="article-title">
                <%= link_to user.username, user_path(user)%>
              </li>
              <li>
                Wins: <%= user.wins %>
              </li>
              <li>
                Losses: <%= user.losses %>
              </li>
              <li>
                <% if user.online == 2 %>
                  <a><%= link_to "Battle", "#", class: "btn btn-success btn-md start-battle",
                           "data-sid" => current_user.id, "data-rip" => user.id %></a>
                <% end %>
              </li>
            </div>
          <% end %>
        </div>
      </ul>
    <% end %>
  <%= will_paginate %>
</div>
