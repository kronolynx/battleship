$("#message_body").val('');
var chatbox = $("#chatbox_<%= @message.battle.id %>");
chatbox.append("<%= j render( partial: @message ) %>");
var chat = $(".chat-history");
chat.scrollTop(chat[0].scrollHeight);
